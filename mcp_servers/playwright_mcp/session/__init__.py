# Playwright MCP Session Management Package
# This __init__.py file marks the directory as a Python package per PEP 420.
# Module-level exports are defined in individual component files (manager.py)
# to avoid circular import issues and maintain clear dependency boundaries.
